<%- include('partials/header') %>
    <h1>Home</h1>
    <p>
        <%= startingContent %>
    </p>
    <% articles.forEach(post=>{ %>
        <h1>
            <%= post.title %>
        </h1>
        <p>
            <%= post.content + '...' %> <a href="/post/<%= post._id %>">Read me</a>
            <% console.log(post.content) %>
        </p>
        <% })%>
            <%- include('partials/footer') %>